#include "SDL.h"
#include "SDL_image.h"
#include <string>

/*
class GameObject
{
public:
	GameObject(int x, int y, int w, int h, std::string texturePath);
	~GameObject();

	void Draw(Renderer* rend);

private:

	int x, y;
	int w, h;

	SDL_Texture* texture;

};
*/